discord:
  enabled: false
  channelId: changeme
  formats:
    chat:
      format: "**{display_name}** » {message}"
    emote:
      format: "_ \\* {display_name} {message}_"
    advancement:
      colour: "#FFAA00"
      # This message is currently delivered in full
      format: "{message}"
    death:
      colour: black
      # This message is currently delivered in full
      format: "{message}"
    serverStarted:
      colour: green
      format: "🟢  Server has started"
    serverStopping:
      colour: red
      format: "🔴  Server is stopping"

# Formatting available: Placeholders, Legacy, Tags
# Do not use placeholders which include player's display name, e.g. %player:displayname%
displayNameFormat: "%player:name%"

# Formatting available: Placeholders, Legacy, Markdown Lite, Tags
chatFormat: "%player:displayname% <gray>»</gray> {text}"

# Formatting available: Placeholders, Legacy, Markdown Lite, Tags
emoteFormat: "<gray><italic>* %player:displayname% {text}</italic></gray>"
